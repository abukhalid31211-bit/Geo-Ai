import { colors, typography, spacing, shadows, borderRadius, animations } from '@theme/index';

export function useTheme() {
  return {
    colors,
    typography,
    spacing,
    shadows,
    borderRadius,
    animations,
  };
}
